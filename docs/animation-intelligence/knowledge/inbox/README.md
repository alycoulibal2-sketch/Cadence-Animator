# Knowledge inbox

Entries written by the watch sessions (`../../watch/Wnn.md`), one JSON file per technique in Part 25's twenty-field shape, named `Wnn-<video>-<slug>.json`. The learning-loop session loads them through `ai/knowledge.js`'s Part 72 gate and moves the accepted ones up a level; nothing here is used by the app until then.
