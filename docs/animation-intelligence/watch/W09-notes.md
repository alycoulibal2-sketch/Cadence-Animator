# W09 notes — videos 81–90 (E. Roblox and Moon Animator · F. VFX, camera and curves)

## The ten videos

- [x] 81. How to ACTUALLY Use VFX in Roblox — Develuper (9:24)
- [x] 82. How to Learn Roblox Aura VFX in 1 Hour — Twist VFX (41:47)
- [x] 83. How to make Cutscenes in Roblox Studio — Jayyy (3:49)
- [x] 84. How To Make Animated Character Cutscenes in Roblox Studio — RKGAM3ZS (15:42)
- [x] 85. The BEST way to learn FX Animation? — Alex Grigg (15:51)
- [ ] 86. #5: Timing | Artistic Principles of VFX — VFX Apprentice (23:10)
- [ ] 87. Ultimate Guide to Camera Movement — StudioBinder (29:09)
- [ ] 88. 7 Rules of Cinematic Framing and Composition — Kellan Reck (9:29)
- [ ] 89. Animate Cameras like a Pro (Blender Tutorial) — CG Boost (23:10)
- [ ] 90. How to Animate with the Graph Editor — Sir Wade Neistadt (21:04)

---

## Video 81 — How to ACTUALLY Use VFX in Roblox (Develuper, 9:24) — 2026-09-12

Watched at `balanced` (92 frames read in full, all scene-change selections; near-static code/UI segments meant most of the 92 were code editor, Studio viewport or a "Top Charts" browse page rather than distinct motion). Transcript via captions, 235 segments.

Five entries written, all specific to concrete techniques shown or narrated, not restatements of "use VFX well": **attachment-anchored VFX with server-authoritative emit** (the video's foundational pair: an Attachment parents the effect to a moving part, and separately, only the server can make a fired effect visible to every player — two different problems solved by the same demo); **animation-marker-triggered VFX emit** (a named marker on the release frame fires paired sound+particles at that exact frame — this is almost exactly Cadence's own Part 39 timing envelope already, with this video's convention landing at `lead ≈ 0`); **raycast-derived impact VFX placement** (the impact clones to the actual raycast hit point, falling back to the aim target on a miss — the presenter calls this out by name as what "makes everything feel way more accurate"); **empty-rig-template as VFX organization scaffold** (a meshless rig used purely to hold folders of attachments/emitters, matched by part name onto a real character at spawn — Cadence has no analogue for "author once, name-match onto any rig" for effect bundles, only for motion via the library); and **ground effect slaved to an invisible proxy part** (an aura's ground ring tracks position only, not rotation, via a separate part someone updates every frame — directly a sibling of `W08-72`'s camera-slaved-to-proxy-part, same underlying idea applied to VFX instead of a camera).

What surprised me: how cleanly the video's own vocabulary (attachment = anchor, marker = trigger, "feels more accurate" = raycast placement) maps onto machinery `ai/vfxspec.js` already has (`anchor`, `timing.event`/`lead`, `EFFECT_ROLES`) — this is a confirming video more than a novel one, much like W01's result. The one clean *gap* it surfaces, twice, is the authoring/runtime boundary: a raycast hit point and a live-updated ground proxy are both **runtime-computed positions with no keyframed representation**, and Cadence's current model (an authored `.cadence` project, no physics, no per-frame recomputation against another object) simply cannot represent either — correctly recorded as "not applicable" rather than invented as a measurement.

Nothing here contradicted an existing knowledge card. No capture candidates — the only "character" on screen is a static-posed R6 robot; no walk, attack or landing worth a reference clip. No new check queued: the one obviously-relevant measurement (`vfx_timing_alignment`, marker-vs-effect-peak distance) is already built (VFX-010) and this video's use of it is a confirmation, not a gap.

## Video 82 — How to Learn Roblox Aura VFX in 1 Hour (Twist VFX, 41:47) — 2026-09-12

Watched at `transcript` only (1131 caption segments), deliberately — a single continuous live-build screen recording where every technique is narrated with exact property names and values as it happens; no frame pass was needed to confirm any of the six entries below, all of which quote the transcript's own precise language. This is the closest thing so far in the whole watchlist to a straight Roblox ParticleEmitter property reference, but the entries below are the specific TECHNIQUES buried in it, not the property list itself (rate/lifetime/speed/spread/shape/flipbook/etc. are Roblox API surface, not craft knowledge, so they were not written up as cards).

Six entries: **phase-offset dual emitters for a persistent glow** (two identical low-rate emitters started half a lifetime apart so their fade cycles sum to roughly flat — the single most sophisticated technique in the video, and a clean case where Cadence's VFXSpec, one emitter per compiled effect, has no field for "a second copy of this at a declared phase offset"; that composition is PNX's job, not VFXSpec's, by the same boundary decision already in the code); **concurrent particle budget = rate × lifetime** (a high rate is not itself spammy if lifetime is short — directly legible against Cadence's own `EFFECT_ROLES` budget-share caps, though VFXSpec does not expose rate/lifetime as separate dials to multiply); **simultaneous-activation baseline defeats authoring-order phase drift** (emitters toggled on at different times while building drift out of phase purely by accident; disable-and-re-enable-together before judging the result, since the real game always activates everything at once — and Cadence's declared, frame-numbered `timing.event` is structurally immune to this exact class of bug, worth recording as a case where the authored/keyframe-native model sidesteps a live-toggle workflow's own failure mode); **per-instance randomization breaks a unison look** (the comma min,max convention on rotation/speed/lifetime, named as a cheap, high-value fix); **a particle must fade at both lifetime extremes or it pops** (near-universal, presenter says he sees it violated constantly); and **a speed floor of 0.001, never a literal 0**, to dodge an orientation-mode clipping bug the presenter can trigger reliably but cannot fully explain.

What surprised me: the phase-offset glow trick is the first technique in this whole watchlist that is a genuine COMPOSITION problem (two synchronized-but-offset emitters) rather than a single-emitter authoring choice — it lands exactly on the VFXSpec/PNX boundary the codebase already draws for other reasons (layers, multi-node graphs), which W08's own throughline flagged from a different angle (Cadence's two systems not always agreeing on scope). Also notable: the simultaneous-activation entry is a rare case of Cadence's data model being *structurally better* than the live-authoring workflow being taught, purely as a side effect of storing timing as declared frames rather than wall-clock toggle history — worth keeping in mind as a positive selling point, not just a gap list.

Nothing contradicted an existing knowledge card (there was no prior card to contradict in this territory — VFX/particle craft is new ground for this corpus, W09 being the first VFX-heavy batch). No capture candidates (a screen recording of Studio's UI, not a performed character motion). No new check queued: none of the six entries above name a quantity `ai/motion.js` could measure from project data — they are either particle-system-internal (outside Cadence's rig/item motion model entirely) or authoring-workflow habits, and are recorded as `not applicable`/`not measurable in this build` rather than stretched into an invented check.

## Video 83 — How to make Cutscenes in Roblox Studio (Jayyy, 3:49) — 2026-09-12

Watched at `balanced` (26 frames, all read — a short video, so the full scene-aware set rather than a subsample), 78 caption segments. A comedic Moon-Animator cutscene demo (a JoJo "THE WORLD" time-stop reference) rather than a serious shot-craft video, so the value here is entirely in the PIPELINE it demonstrates, not in any camera-language lesson.

Two entries: **camera keyframes relative to a declared reference object** (Moon Animator's camera is explicitly keyed against a "reference object" set to the rig's HumanoidRootPart — this is, concretely, Cadence's own track-space concept, `set_track_space`/`get_track_space`, under a different name; nothing new needs building, only correctly setting the camera's `@origin` track space to match); and **the animation-marker mechanism as a generic side-effect trigger bus**, not a VFX-only feature — this video reuses the exact `GetMarkerReachedSignal` pattern from W09-81's VFX entry to drive a subtitle system instead (show text at a marker, auto-hide after a fixed wait), and says outright "you can use the same technique for adding VFX," which is good outside confirmation that a marker's CONSUMER is meant to be swappable — matching how Cadence's own `ai/events.js` marker model is already content-agnostic rather than VFX-specific.

What surprised me: this is the first video in the batch to show the camera-authoring half of the "E. Roblox and Moon Animator" section, and it lands as a confirmation of Cadence's track-space feature rather than a gap — worth flagging because Cadence's own docs don't obviously connect "track space" to "camera cutscene portability," and this video makes that connection concrete from the outside. Nothing contradicted an existing card. No capture candidates (a static-posed meme cutscene, no locomotion or combat worth a reference clip). No new check queued — both entries are authoring/architecture patterns, not measurable motion quantities.

## Video 84 — How To Make Animated Character Cutscenes in Roblox Studio (RKGAM3ZS, 15:42) — 2026-09-12

Watched at `balanced` (67 frames, all read — a live-code Studio tutorial, so most frames are code editor or a blocky R6 rig, but several landed on genuinely readable full-script text that let me quote exact variable names rather than paraphrase). 340 caption segments. This is the batch's clearest single example of "the shot model's reference" the plan called out: one script owns character snap-in, a hard camera cut, a paired NPC+player animation, and a precisely-gated hand-off back to the player, all in one place.

Four entries: **the player is snapped to a fixed cutscene pose before the camera cuts** (`HumanoidRootPart.CFrame` hard-set to a pre-placed invisible part, WalkSpeed/JumpHeight zeroed, the instant the cutscene triggers — removes the player's actual position as a variable the rest of the scene has to survive); **`CameraType.Scriptable` for a hard cutscene takeover**, released back to `Custom` at the end — the direct Roblox mechanism for seizing and returning the player's view, confirmed reading the live script (`cam.CameraType = Enum.CameraType.Scriptable`, `cam.CFrame = cam1.CFrame`, later `Enum.CameraType.Custom`); **a blocking `Stopped:Wait()` for the one hand-off that must be exact**, contrasted directly against every OTHER beat in the same script being sequenced with guessed `task.wait(N)` calls — the presenter's own on-camera bug (referencing the bare `Stopped` event with no `:Wait()`, which does nothing) and fix is a clean, concrete case of "exact, not estimated" winning out even in an otherwise-informal script; and **a temporary visible light as a facing-proxy** while placing an invisible camera part, deleted before shipping — which I recorded as a case where Cadence's own `get_facing`/`get_rotation_degrees` plus a live 3D viewport are a genuine improvement on the manual workaround being taught, not a gap to close.

Also confirms, rather than duplicates, an existing proposal: the video's own mid-tutorial bug (arms not moving during the reaction animation until "you need to go to set animation priority and make it an action") is the exact failure mode `W08-78-animation-priority-determines-track-override.json` (proposed, not yet merged) already describes — independent confirmation from a fifth-ish source in this corpus, not written up again as its own card.

What surprised me: this video is the first in the batch where I could read a FULL, final script directly off frames with high confidence (three separate frames landed on the complete, un-scrolled script), which let every example above cite the actual code rather than only the narration — worth noting for future sessions that a "live-code tutorial" video, unlike a live-build VFX video like W09-82, is often better served by reading frames than skipping to transcript-only, precisely because the code itself (not just what's said about it) is the evidence. Nothing else contradicted an existing card. No capture candidates — both animations are explicitly, deliberately placeholder-bad ("terrible animation", presenter's own words), not reference-quality motion. No new check queued: all four entries are authoring/runtime-script patterns outside `ai/motion.js`'s keyframed-motion domain.

## Video 85 — The BEST way to learn FX Animation? (Alex Grigg // Animation for Anyone, 15:51) — 2026-09-12

Watched at `balanced` (100 frames read, capped from 177 candidates — a professional 2D animator's own craft talk, opening with reference-quality clips from Ponyo, Batman: TAS, Spirited Away, and Princess Mononoke before his own Procreate Dreams tutorial and short-film work). 463 caption segments. The first genuinely non-Roblox, non-tutorial video in this batch, and the richest single video so far in original craft insight — matching the plan's one-line gloss ("effects as motion: source, energy, dissipation") almost exactly.

Five entries: **motion-path-first, volume-second authoring** (plan a soft body's motion as a single dragged "dot" before drawing any deforming volume around it, letting the edges lag like a magnet pulling goo — ties to the corpus's existing chain-lag cards but reframes lag as a deliberate TWO-PASS WORKFLOW rather than only a measured property); **legible vs. ambiguous energy source as an authorial choice** (the exact "source" the plan called out — the SAME blob technique reads as "magical" with no source staged, physically-caused with a gravity/force cue, or characterful/alive with a held pause and no external cause — confirmed against both his tutorial exercise and his Phantom Limb short); **traveling energy as a phase offset, not a midpoint interpolation** (the single best insight in the video: naively blending a wave's two keyframes moves the WHOLE material halfway at once, which is visibly wrong, because a real wave only displaces PART of the material at any instant — confirmed with three independent worked examples, including Felix Colgrave's Double King flag); **a rough timing pass retraced specifically for spacing quality**, as a distinct second pass from ordinary staged-keyframe workflows; and **a sticky trailing contact point as a pure-timing signal for material adhesion** (holding a trailing contact past what momentum alone would produce, contrasted explicitly against Play-Doh, to tell the audience the material feels tacky — with no new visual detail at all).

What surprised me: the "traveling energy" entry is a structural sibling of W09-82's phase-offset-dual-emitter finding from a completely different domain (hand-drawn 2D liquid vs. Roblox particle systems) — both correct the same naive mistake (treating a system as one state interpolated in time, rather than as several phase-offset instances of the same cycle happening at once). That is now three independent confirmations of the same underlying idea across this batch (also echoing CLAUDE.md's own breakdown-bias rule for rigid rigs), which is worth a building session's attention as a structural note, not just three separate cards. Two things confirmed rather than duplicated existing cards: the video's own volume-conservation rule ("stretch it out... it also needs to get thinner") restates `squash_stretch.json` exactly, and its "watch real reference" advice restates `physical_reference_timing_method` (v9) — both left as confirmations here, not rewritten.

check: sticky_contact_hold_duration_vs_momentum_baseline — measure a trailing effector's actual contact-hold duration against what an unconstrained momentum decay alone would predict; a hold significantly longer than that baseline is the measurable signature of the "sticky material" read named in `sticky_trailing_contact_signals_material_adhesion` — **blocked**: needs a counterfactual momentum-only simulation to compare against, which this build does not have (source: video 85 @ 11:22–11:36).

No capture candidates (2D hand-drawn throughout, no rig performance). Nothing contradicted an existing card.

