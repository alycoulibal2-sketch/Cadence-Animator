# Cadence watch session W07 of 10 — videos 61–70

Sections: D. Anime and stylised action · E. Roblox and Moon Animator. About 145 minutes of video; 6 of the ten are watched with full frames.

You are a FRESH session with one job: watch these ten videos and turn what they teach into
files the Cadence Animation Intelligence programme can use. Nothing else — do not build features,
do not edit code. Other sessions are doing other batches at the same time, so you write only
your own files (§4).

## 0. Setup (two minutes)

1. Repo: the `Cadence-Animator` checkout (desktop `C:\Users\User\CadenceAnimator`; laptop the same
   repo under `C:\Users\alyco\`). `git fetch origin && git checkout animation-intelligence && git pull`.
2. Read `docs/animation-intelligence/watch/README.md` (how batches merge). If
   `docs/animation-intelligence/LESSONS.md` exists, skim it so you do not repeat what earlier sessions
   learned; skim the file names in `docs/animation-intelligence/knowledge/` and `knowledge/inbox/`.
3. The `/watch` skill is installed and configured. Its folder is
   `C:\Users\User\.claude\plugins\marketplaces\claude-video\skills\watch` (on the laptop the same path
   under `C:\Users\alyco`). On Windows every `python3` in its instructions is `python`. Preflight once:
   `python "<SKILL_DIR>/scripts/setup.py" --check` (exit 0 = ready). DNS on the desktop drops now and
   then; an `ENOTFOUND` from yt-dlp is a retry, not a failure.

## 1. The frame budget — the reason this list is split into ten sessions

Frames are images in your context and each one costs. Ten videos is the budget of one session at the
detail written next to each video:

- `balanced` — scene-aware frames, cap 100. For tutorials that SHOW motion. Read every frame; that is
  what the small batch buys. If a video yields more than 80 frames, re-run it with `--max-frames 60`.
- `efficient` — up to 50 keyframes. For long videos or ones that are mostly words with some visuals.
- `transcript` — no frames. For talks and analyses. If the transcript points at a demo, frame only
  those minutes: `--start MM:SS --end MM:SS --detail efficient`.

Watch one video at a time and write its entries before starting the next. When the context gets
heavy, stop: tick what you finished in your notes file, commit, and say which video is next. The
next session runs this same prompt and continues from the first unticked video.

## 2. The ten videos

- [ ] 61. Types of Frames in Animation — NobleFrugal Studio (9:53) — https://youtu.be/EMXSFQ4pdUM
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: keys, extremes, breakdowns, in-betweens, smears, holds — the pose roles of Part 28
- [ ] 62. Every (Anime) Animation Technique Explained in 12 Minutes — SinChi (13:01) — https://youtu.be/iMV9Tlpo1wY
      detail: `efficient` — up to 50 keyframes: long, or mostly words with some visuals
      teaches: the anime vocabulary in one pass
- [ ] 63. The Art of Animators (or Sakuga) — RCAnime (7:44) — https://youtu.be/-aChpK2jcnQ
      detail: `efficient` — up to 50 keyframes: long, or mostly words with some visuals
      teaches: what sakuga values: timing contrast, weight, spacing
- [ ] 64. Sakuga OVERKILL! | Animation Analysis: Stark vs Dragon — MankoMan (25:12) — https://youtu.be/ibavOZYfsnQ
      detail: `efficient` — up to 50 keyframes: long, or mostly words with some visuals
      teaches: a shot-by-shot analysis of a heavy attack sequence
- [ ] 65. I Spent 30 Days ANIMATING this FIGHT SCENE!! — Shrimpy (16:31) — https://youtu.be/OVPfRoIP69Q
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: a fight scene's planning, blocking and polish over time
- [ ] 66. PWOW Workshop - Introduction to Animation Breakdowns — Toniko Pantoja (16:25) — https://youtu.be/wdPbiy-8BRo
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: breakdown drawings as the route between keys
- [ ] 67. Moon Animator 2 Basics - Official Tutorial — six (4:41) — https://youtu.be/q8tGNMo_jHg
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: the tool Cadence replaces, from its author
- [ ] 68. Roblox ANIMATION Guide #1 - Moon Animator (2026) — Nisky (12:07) — https://youtu.be/267aFypaeWU
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: how Roblox animators actually work today
- [ ] 69. Roblox Animation in Blender: Full Beginner Guide [2026] — Nisky (7:46) — https://youtu.be/eg6COxaPCyo
      detail: `balanced` — scene-aware frames (cap 100): this one SHOWS motion — read every frame
      teaches: the Blender route and its export shape
- [ ] 70. Roblox Animation in Blender: Advanced Guide (2026) — Nisky (31:26) — https://youtu.be/EM9u4gIHoRg
      detail: `efficient` — up to 50 keyframes: long, or mostly words with some visuals
      teaches: advanced Roblox animation habits worth measuring against

## 3. What to write, per video (the deliverable)

1. **Knowledge entries** — one JSON file per technique or rule the video teaches (usually one to
   four per video) at `docs/animation-intelligence/knowledge/inbox/W07-<video-number>-<slug>.json`,
   in Part 25's exact twenty-field shape (the loader refuses anything else):

   ```
   concept, category, definition, purpose, visible_effect, psychological_or_gameplay_effect, physical_interpretation, use_cases, non_use_cases, interactions, style_variations, cadence_representation, control_surface, detection_and_measurement_methods, generation_or_modification_methods, failure_modes, roblox_considerations, performance_implications, examples, evidence_status
   ```

   `category` is one of `essential`, `advanced`, `optional`, `specialized`, `experimental`. Every
   field is present and specific; write `"not applicable"` rather than leaving one empty.
   `evidence_status` names the source exactly: `"video: <url> @ MM:SS — <what was shown or said>;
   experimental until validated"`. `detection_and_measurement_methods` names what `ai/motion.js`
   could measure (velocity, acceleration, jerk, curvature, contact drift, chain lead/lag, key density)
   or says `"not measurable in this build"` — never invent a measurement. If the MCP tool
   `propose_knowledge_entry` exists (the learning loop has been built), call it instead of writing
   the file: it validates the shape and files the entry.
2. **Capture candidates** — if the video SHOWS a motion worth having as a measured reference (a
   clean walk, a heavy attack, a jump and landing, a hit reaction), add a line to your notes: URL,
   start–end timestamps, action type, why. The user captures it later with Roblox Studio's
   Animation Capture; you do not.
3. **Checks the review or planner should enforce** — a line in your notes of the form
   `check: <name> — <what to measure> — <threshold or convention> — <source video @ time>`. These
   become measured checks in a later session; a check with no measurable quantity is a note, not a
   check — say which.
4. **Your notes file** `docs/animation-intelligence/watch/W07-notes.md`: a copy of the ten-video list at
   the top with ticks, then per video one dated paragraph in your own words — specific (frame
   counts, ratios, the order in which body parts move, the exact rule of thumb), what surprised you,
   what contradicted an existing knowledge card (name the card), and the entries you wrote.

## 4. Rules

- Write ONLY `docs/animation-intelligence/watch/W07-notes.md` and
  `docs/animation-intelligence/knowledge/inbox/W07-*.json`. Do not edit `WATCHLIST.md`, `LESSONS.md`,
  the knowledge folder outside `inbox/`, or any code — the learning-loop session merges every
  batch. This is what lets ten sessions run at once without conflicts.
- Specific beats general. "Anticipation takes 25–40 % of the action's duration in the examples
  shown" is an entry; "anticipation matters" is not.
- Never claim a measurement the video did not show. A presenter's opinion is recorded as the
  presenter's, with their name.
- The user's three constraints for anything that ends up in the tool: exact (computed, never
  estimated), no learning required of the user, full power (a starting recipe is never a ceiling).
- No purchases, no new tools, no API keys, no code.
- Commit at the end, your files only: `git add docs/animation-intelligence/watch/W07-notes.md
  docs/animation-intelligence/knowledge/inbox/W07-*.json`, then commit, then
  `git pull --rebase origin animation-intelligence`, then push. Never stage `site/` or the stray root
  files (`cd.html`, `sp*.html`, `yt.html`, `ids.txt`, `posts.json`).

## 5. End of the session

Say which of the ten were watched, how many entries, capture candidates and checks were written,
what contradicted an existing card, and which video the next session should start from.
